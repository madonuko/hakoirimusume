---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<div class:list={['split-right', className]}>
  <slot />
</div>

<style>
  .split-right {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }
</style>
